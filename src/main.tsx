// Browser entry point: mounts App into #root and pulls in the one stylesheet.
// Everything else in this repo hangs off these ten lines.
import { StrictMode } from 'react'
import { createRoot } from 'react-dom/client'
import './index.css'
import App from './App.tsx'

createRoot(document.getElementById('root')!).render(
  <StrictMode>
    <App />
  </StrictMode>,
)
