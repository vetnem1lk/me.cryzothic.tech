# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
*.tsbuildinfo

# local agent context (not part of the public repo)
CLAUDE.md
graphify-out/
cryx.deploy.json
.superpowers/
gate-shots/

# 3D source/optimized assets never ship through git — they live under /g2/v1/
# on the host (versioned URL, immutable cache); the poster render is the one
# committable artifact of the pipeline.
*.glb
*.ktx2
*.fbx

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# server secrets & private prompts (public repo!)
.env*
!.env.example
server/prompts/*
!server/prompts/README.md
